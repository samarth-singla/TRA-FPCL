# TRA FPCL — Project Snapshot
*Last updated: March 6, 2026 (v4 — Offline sync + Profile screen added). Hand this file to a new chat to continue work.*

---

## 1. What This Project Is

**TRA FPCL** (Tribal Rural Agripreneur - Farmer Producer Company Limited) is a **Flutter mobile app** for managing a rural agriculture supply chain in India. It connects four types of users:

| Role | Full Name | What They Do |
|---|---|---|
| **RAE** | Rural Agripreneur | Field agent who buys inputs (seeds, fertilisers, pesticides) for farmers, places orders |
| **SME** | Subject Matter Expert / District Advisor | Supervises RAEs in a district, handles farmer issues, monitors field performance |
| **ADMIN** | FPCL Admin | Manages inventory, approves orders, oversees the whole operation |
| **SUPPLIER** | Input Supplier | Fulfils orders, manages stock |

**Tech stack (actual, not the brief):**
- **Flutter** (Dart) — cross-platform: Android (mobile app) + Chrome (admin web portal). Two entry points: `lib/main.dart` (mobile) and `lib/admin_main.dart` (web).
- **Firebase Auth** — phone OTP login only. Firebase UID (a TEXT string) is the primary key everywhere.
- **Supabase** — used purely as a Postgres database (NOT for auth). RLS is **disabled on all tables** because `auth.uid()` is always NULL when using Firebase Auth instead of Supabase Auth.
- **Provider** — state management (used only for `CartService` so far)
- **shared_preferences** — caches the user's role locally for offline/fast routing
- **sqflite** — local SQLite database for offline data cache (mobile only; no-op on web)
- **connectivity_plus** — detects online/offline status for sync decisions
- **path** — path utilities for sqflite database file location
- **firebase_options.dart** — generated by `flutterfire configure`, required for web Firebase initialisation

**Critical architectural note:** Firebase Auth and Supabase Auth are completely separate systems. This app uses Firebase identity, so Supabase's `auth.uid()` always returns NULL. All tables must have `DISABLE ROW LEVEL SECURITY`. Security is enforced by Firebase Auth on the client side.

---

## 2. Infrastructure & Credentials

### Firebase
- Project is already configured in `android/app/google-services.json`
- Phone auth is enabled
- **Test phone numbers** (the ONLY numbers that work without billing):
  - `+919999999999` → OTP `123456`
  - `+918882207841` → OTP `112233`
- Real phone numbers require Blaze (pay-as-you-go) plan. Don't use real numbers for dev.
- To add more test numbers: Firebase Console → Authentication → Sign-in method → Phone → Test phone numbers

### Supabase (NEW project — created this session)
- **URL:** `https://hwlwxzyrcaxjjlnnokxk.supabase.co`
- **Anon key:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imh3bHd4enlyY2F4ampsbm5va3hrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI3NjcxMDgsImV4cCI6MjA4ODM0MzEwOH0.RnR9KTt-tUVej8qSkOMCpiJy6AzvVzOiJrBZl2r43Fg`
- **Schema was successfully applied** (`SUPABASE_TABLES.sql` ran with "Success. No rows returned")
- All tables use `DISABLE ROW LEVEL SECURITY`

### Database Tables (all created)
| Table | Purpose |
|---|---|
| `profiles` | One row per user. Columns: `uid TEXT PK`, `phone`, `role`, `name`, `email`, `district` |
| `products` | Product catalogue (seeds, fertilisers, pesticides) — sample data seeded |
| `orders` | Orders placed by RAEs |
| `order_items` | Line items for each order |
| `notifications` | In-app notifications per user |
| `conversations` | RAE ↔ SME chat threads |
| `issues` | Complaints/issues raised by RAEs, handled by SMEs |
| `sme_metrics` | Per-SME performance stats (active RAEs, villages, farmers) |

---

## 3. Project File Structure

```
lib/
├── main.dart                          ← Mobile app entry, AuthWrapper, DashboardRouter, DashboardShell (DashboardShell no longer used for RAE)
├── admin_main.dart                    ← Admin web portal entry, AdminAuthWrapper, AdminLoginScreen, AdminOtpScreen
├── firebase_options.dart              ← Generated by `flutterfire configure` (needed for web)
├── services/
│   ├── auth_service.dart              ← Firebase OTP + Supabase profile sync + role caching
│   ├── cart_service.dart              ← Cart state (Provider ChangeNotifier)
│   ├── offline_sync_service.dart      ← SQLite offline cache (profiles, products, orders, notifications)
│   ├── sme_service.dart               ← SME dashboard data (models + Supabase queries)
│   ├── supplier_service.dart          ← Supplier data layer (BulkPurchaseOrder, SupplierStats, etc.)
│   └── admin_service.dart             ← Admin data layer (AdminStats, OrderSummary, DistrictStat)
└── screens/
    ├── auth/
    │   ├── phone_login_screen.dart    ← Role selector (RAE/SME/SUPPLIER — no ADMIN, no RAE ID field) + phone input
    │   └── otp_verification_screen.dart ← 6-box OTP entry
    ├── catalog/
    │   ├── product_catalog_screen.dart ← Product grid for RAE
    │   └── shopping_cart_screen.dart   ← Cart for RAE
    ├── dashboard/
    │   ├── rae_dashboard.dart         ← RAE dashboard (green theme)
    │   ├── sme_dashboard.dart         ← SME dashboard (purple theme)
    │   └── supplier_dashboard.dart    ← Supplier dashboard (indigo theme)
    ├── profile/
    │   └── profile_screen.dart        ← Universal profile screen (all roles; edit name/email/district; sync status)
    ├── supplier/
    │   └── catalogue_management_screen.dart ← Product catalogue CRUD for Supplier
    └── admin/
        ├── admin_dashboard.dart       ← Admin main dashboard (blue theme, web-optimised)
        └── order_approval_screen.dart ← Order approval with Pending/Approved tabs
```

---

## 4. Auth Flow (How Login Works)

```
PhoneLoginScreen  ← AuthWrapper routes here directly (no intermediate splash/landing screen)
  1. User selects role (RAE/SME/SUPPLIER) and enters 10-digit phone
  2. _authService.setRole(_selectedRole)   ← MUST be before sendOTP (race condition fix)
  3. await _authService.sendOTP(phoneNumber)
     ├── Uses Completer — waits for codeSent callback before returning
     ├── _verificationId is guaranteed set when function returns
     └── verificationFailed with BILLING_NOT_ENABLED → clear error shown
  4. Navigator.push → OTPVerificationScreen

OTPVerificationScreen
  5. User enters 6-digit OTP
  6. await _authService.verifyOTPWithRole(otp, role)
     ├── Creates PhoneAuthCredential
     ├── _signInWithCredential → Firebase sign in
     ├── _ensureProfileExists → upsert into Supabase profiles table
     └── cacheRole(role) → SharedPreferences

AuthWrapper (listens to Firebase authStateChanges)
  7. Firebase user != null → DashboardRouter

DashboardRouter
  8. _getUserProfile():
     ├── Fast path: SharedPreferences cache → returns {role, uid}
     └── Fallback: Supabase profiles table → caches result
  9. switch(role) → RAEDashboard / SMEDashboard / SupplierDashboard / (ADMIN shows web portal message)
```

---

## 5. What Has Been Built

### ✅ Auth System
- Phone OTP login with Firebase
- Role selection on login screen
- Race condition fixed: `sendOTP()` uses `Completer`, only returns after Firebase `codeSent` fires
- Role cached in SharedPreferences for instant offline routing
- Sign-out clears cache
- Error recovery screen when profile can't be loaded (instead of silently defaulting to RAE)

### ✅ RAE Dashboard (`rae_dashboard.dart`)
- Green-themed dashboard with its own built-in green header (no `DashboardShell` wrapper)
- Shows order history, product catalogue shortcut
- Returned directly from `DashboardRouter` (same pattern as SME and Supplier)

### ✅ RAE Product Catalogue & Cart
- `product_catalog_screen.dart` — browse products by category, add to cart
- `shopping_cart_screen.dart` — view cart, place order
- `CartService` (Provider) manages cart state

### ✅ SME Dashboard (`sme_dashboard.dart`)
Built to match Figma wireframes exactly:
- **Purple gradient header** (`#7B2FDC` → `#5B1FA8`) with district advisor title
- **2×2 stat cards grid** — Active RAEs, Open Issues, Villages Covered, Farmers Served
- **Active Conversations** section — realtime stream from Supabase, green left border for unread
- **Issues list** — open=orange badge, resolved=green badge. Tapping shows "View Details" dialog with resolve action
- **District Performance** section — overview of RAE activity
- **Activity Log** section — recent events
- **Dark green FAB** (`#1B8C4E`) with bottom sheet menu

### ✅ Supplier Dashboard (`supplier_dashboard.dart`) — NEW
Built to match mockscreen designs:
- **Indigo gradient header** (`#4F46E5 → #3730A3`) with Supplier ID
- **2×2 stat cards** — Bulk POs, RAE Orders, In Transit, Pending Invoices
- **Quick Actions** — Manage Catalogue, View Reports
- **Bulk Purchase Orders** — expandable cards grouped by district; each expands to show individual RAE orders
- **Dispatch flow** — "Ready to Dispatch" POs show dispatch dialog (driver name, vehicle, consignment)
- **Invoice flow** — "Pending Invoices" POs show "Generate Individual Invoices" button
- **Green FAB** with bottom sheet menu

### ✅ Catalogue Management Screen (`catalogue_management_screen.dart`) — NEW
- Navigated from Supplier Dashboard → Manage Catalogue
- Indigo header with back button
- 3 mini stat cards (Total Products, Active Items, Categories)
- Search bar + Add button, horizontal category filter chips
- Product cards with Deactivate / Edit / Delete actions
- Full Add/Edit dialog with category dropdown, price, unit, stock

### ✅ Supplier Service (`supplier_service.dart`) — NEW
Models: `BulkPurchaseOrder`, `RaeOrderEntry`, `OrderLineItem`, `SupplierStats`, `SupplierPerformance`, `SupplierProduct`

Methods: `getStats()`, `getBulkOrders()` (groups by district), `getPerformance()`, `getProducts()`, `toggleProductActive()`, `deleteProduct()`, `updateProduct()`, `addProduct()`, `dispatchOrders()`, `generateInvoices()`

### ✅ FPCL Admin Web Portal — NEW
A **separate Flutter web application** (entry: `lib/admin_main.dart`).

**Run with:** `flutter run -t lib/admin_main.dart -d chrome`

**AdminAuthWrapper** — listens to Firebase auth, verifies ADMIN role from Supabase/cache, shows Unauthorized screen if not ADMIN.

**Admin Login Screen** — card-style UI (blue theme), `+91` phone prefix, routes to OTP screen. OTP screen has 6-box input with 60s resend timer.

**Admin Dashboard (`admin_dashboard.dart`):**
- Solid blue header (`#2563EB`)
- Compact 2×2 stat cards — Pending Orders, Approved Today, Active RAEs, Total Orders
- Quick Actions grid — Approve Orders, Reports, RAE Management, Suppliers
- Pending Approvals inline list (max 5) with inline Approve/Reject buttons
- District Performance table
- Green FAB with expandable menu

**Order Approval Screen (`order_approval_screen.dart`):**
- Segmented tab bar: Pending (N) | Approved (N)
- Pending tab: order cards with View Details dialog, Approve + Reject actions with confirmation dialogs
- Approved tab: order cards with Generate PO button

### ✅ Admin Service (`admin_service.dart`) — NEW
Models: `AdminStats`, `OrderSummary`, `DistrictStat`

Methods: `getAdminProfile()`, `getStats()`, `getPendingOrders()`, `getAllOrders()`, `getDistrictPerformance()`, `approveOrder()` (sets status → `'confirmed'`, stores approver in notes), `rejectOrder()`

### ✅ Mobile App — Login Screen Cleanup (v3)
- **Splash screen removed**: `AuthWrapper` now routes directly to `PhoneLoginScreen` instead of an intermediate `LoginScreen` widget with a "Sign In with Phone" button
- `phone_login_screen.dart`: `_roleMap` now only contains `['RAE', 'SME', 'SUPPLIER']` — FPCL Admin option fully removed (no dialog, no special-case code)
- `phone_login_screen.dart`: RAE ID field (`_raeIdController`) fully removed
- `main.dart` ADMIN case (for users who somehow have ADMIN role on mobile): still shows "Admin Portal — Web Only" message screen with Sign Out button

### ✅ RAE Dashboard — Redundant Header Removed (v3)
- `DashboardRouter` now returns `RAEDashboard()` directly (no `DashboardShell` wrapper)
- The violet/theme-coloured `AppBar` from `DashboardShell` is gone; the RAE dashboard's own green header is the only header
- Consistent with how `SMEDashboard` and `SupplierDashboard` are returned

### ✅ Offline Sync (`offline_sync_service.dart`) — NEW (v4)
Singleton service. Mobile-only (sqflite not supported on web — guarded by `kIsWeb` check).

- **`init()`** — opens/creates SQLite DB (`tra_fpcl_offline.db`) with tables: `profiles`, `products`, `orders`, `order_items`, `notifications`
- **`syncAll(uid)`** — parallel sync of all tables from Supabase; records timestamp in SharedPreferences; safe to call when offline (no-op)
- **`isOnline()`** — uses `connectivity_plus` to check WiFi/mobile/ethernet
- **`getLocalProfile(uid)`** / **`getLocalProducts()`** / **`getLocalOrders(uid)`** / **`getLocalNotifications(uid)`** — offline reads from SQLite
- **`lastSyncTime()`** — returns `DateTime?` of last successful sync
- Auto-triggered in `main.dart`'s `DashboardRouter._getUserProfile()` as a background call after routing (`.ignore()` — non-blocking)
- Manually triggerable from the Profile Screen via **Sync Now** button

### ✅ Profile Screen (`screens/profile/profile_screen.dart`) — NEW (v4)
Universal screen shared by all roles (RAE/SME/SUPPLIER/ADMIN). Accessed via:
- **RAE**: `Icons.person_outline` button in header + "My Profile" in drawer + "My Profile" in FAB menu
- **SME**: "My Profile" in FAB bottom sheet
- **Supplier**: "My Profile" in FAB bottom sheet
- **Admin**: "My Profile" in FAB bottom sheet

Features:
- Accent colour per role (green RAE, purple SME, indigo Supplier, blue Admin)
- Avatar circle with initials
- Read-only: phone number, role badge
- Editable: full name, email, district — saved to Supabase and synced to local cache
- Offline sync card — shows online/offline status, last sync time, "Sync Now" button
- Sign Out button with confirmation dialog
- Loads from local SQLite cache first; refreshes from Supabase if online

### ✅ Bug Fixes
- `auth_service.dart`: added `import 'dart:io'` (was missing → `SocketException isn't a type` error)
- `supplier_dashboard.dart`: removed `const` from `RichText` with non-const `TextSpan` children
- `admin_service.dart`: removed `const` from `OrderSummary` constructors containing `DateTime`

---

## 6. What Is Left (TODO)

### 🟡 Medium Priority — Incomplete Features

#### Chat Screen (within SME dashboard)
- Tapping a conversation currently shows: `"Chat interface – Coming Soon"` SnackBar
- Need to build: `lib/screens/chat/chat_screen.dart`
- Need to create: `messages` table in Supabase (id, conversation_id, sender_uid, content, created_at)
- Realtime messaging using Supabase streams

#### Farmer Registration (RAE flow)
- RAEs should be able to register farmers they serve
- New table needed: `farmers` (id, rae_uid, name, village, phone, crop_type, land_area)

#### Order Flow Completion
- RAE can add to cart and place order (basic flow exists)
- Missing: Order status updates visible to RAE after Admin approves
- Missing: Supplier assignment and fulfilment confirmation back to RAE

#### Advisory / Content
- SME should be able to send advisories/alerts to RAEs in their district
- New table needed: `advisories` (id, sme_uid, district, title, content, created_at)

#### Admin — Reports & RAE Management screens
- Quick Action buttons exist in Admin Dashboard but currently show a SnackBar placeholder
- Need: proper Reports screen with charts, RAE Management list screen

### 🟢 Low Priority / Polish
- **Push notifications** — Firebase Cloud Messaging not integrated
- **SME Dashboard chat** — conversations are view-only; full chat not built

---

## 7. Key Code Patterns to Follow

### Adding a new dashboard
1. Create `lib/screens/dashboard/xxx_dashboard.dart` — give it its own full `Scaffold` with a coloured header (like `rae_dashboard.dart`, `sme_dashboard.dart`, `supplier_dashboard.dart`). `DashboardShell` exists in `main.dart` but is no longer used — avoid wrapping new dashboards in it.
2. Create `lib/services/xxx_service.dart` — singleton pattern (`static final _instance = ...`)
3. Import in `lib/main.dart` and add `case 'XXX':` in `DashboardRouter`'s switch

### Querying Supabase
```dart
// Always filter by Firebase UID (TEXT), never by Supabase auth
final uid = firebase_auth.FirebaseAuth.instance.currentUser!.uid;
final data = await Supabase.instance.client
    .from('your_table')
    .select()
    .eq('uid_column', uid)
    .order('created_at', ascending: false);
```

### Realtime streams
```dart
Supabase.instance.client
    .from('issues')
    .stream(primaryKey: ['id'])
    // Client-side filter (Supabase stream doesn't support .eq() server-side)
    .map((rows) => rows.where((r) => r['sme_uid'] == uid).toList())
```

### Role caching pattern
```dart
// On login — set role BEFORE sendOTP
_authService.setRole(_selectedRole);
await _authService.sendOTP(phoneNumber);

// On verify — auto-cached inside _ensureProfileExists

// On dashboard load — read from SharedPreferences first (fast path)
final cachedRole = await AuthService().getCachedRole();
```

---

## 8. Running the Project

### Mobile App (Android)

```bash
# Install dependencies
flutter pub get

# Run on connected Android device / emulator
flutter run

# Hot reload (while running)
r   # hot reload
R   # hot restart (clears state)
q   # quit
```

**Test logins (mobile):**
- Phone: `+919999999999`, Role: `RAE`, OTP: `123456` → green RAE dashboard
- Phone: `+919999999999`, Role: `SME`, OTP: `123456` → purple SME dashboard
- Phone: `+919999999999`, Role: `SUPPLIER`, OTP: `123456` → indigo Supplier dashboard
- Phone: `+918882207841`, any mobile role, OTP: `112233`

### Admin Web Portal (Chrome)

```bash
# One-time setup: generate firebase_options.dart
dart pub global activate flutterfire_cli
export PATH="$PATH:$HOME/.pub-cache/bin"
flutterfire configure --project=tra-fpcl-33738 --platforms=android,web

# Run admin portal
flutter run -t lib/admin_main.dart -d chrome

# Build for deployment
flutter build web --target lib/admin_main.dart --output build/admin_web
```

**If you see a blank white screen in Chrome:** `firebase_options.dart` is missing — run `flutterfire configure` above.

**If you see "Could not load your profile" on mobile:** Sign out and sign back in — `_ensureProfileExists` will auto-create the profile row.

---

## 9. Dependencies (`pubspec.yaml`)

```yaml
dependencies:
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  supabase_flutter: ^2.9.2
  provider: ^6.1.1
  shared_preferences: ^2.3.3
  sqflite: ^2.3.3+1
  connectivity_plus: ^6.0.5
  path: ^1.9.0
  cupertino_icons: ^1.0.8
```
